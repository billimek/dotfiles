# Home Manager modules - auto-discovered by flake-module.nix
# Each .nix file in this directory becomes a module with an enable option
{
  # Modules are auto-discovered. This file exists for manual imports if needed.
}
